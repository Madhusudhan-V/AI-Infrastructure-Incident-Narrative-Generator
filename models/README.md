# Models

No model binary is committed by default. The first demo works with deterministic rules and an optional local Ollama LLM. Trained anomaly models should be saved locally and excluded from Git unless small enough and intentionally versioned.
