# Dataset

The project previously prepared an HDFS log dataset into a Parquet file with 11,175,629 parsed lines and 0 failed lines. Keep large raw datasets outside Git; store preprocessing scripts and documentation here, and add a local path in your environment when running experiments.
